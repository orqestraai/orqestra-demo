# Demo Harness Scripts

This directory is reserved for deterministic ADR-121 demo and nightly assertion helpers.
Reset logic intentionally does not live here: `orqestra demo reset` is owned by the dev
CLI under ticket #4546.
